@model lab1mvc.Models.Student
@{
    ViewData["Title"] = "Edit Student";
}

<h2 class="text-warning mb-3">Edit Student</h2>

<form asp-action="Edit" asp-controller="Student" method="post">
    @* to know who should send ssn *@
    <input type="hidden" asp-for="SSN" />

    <partial name="_StudentForm" model="Model" />

    <button type="submit" class="btn btn-primary">Save Changes</button>
    <a asp-action="GetAll" class="btn btn-secondary ms-2">Cancel</a>
</form>
section Scripts {
<partial name="_ValidationScriptsPartial" />
}


































@* lab3 *@
@* @model lab1mvc.Models.Student
@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers

<h2>Edit Student</h2>

<form asp-action="Edit" asp-controller="Student" method="post">
    <input type="hidden" asp-for="SSN" />

    <div class="mb-3">
        <label>Name</label>
        <input asp-for="Name" class="form-control" />
    </div>

    <div class="mb-3">
        <label>Age</label>
        <input asp-for="Age" class="form-control" />
    </div>

    <div class="mb-3">
        <label>Email</label>
        <input asp-for="Email" class="form-control" />
    </div>

    <div class="mb-3">
        <label>Image</label>
        <input asp-for="Image" class="form-control" />
    </div>

    <div class="mb-3">
        <label>Address</label>
        <input asp-for="Address" class="form-control" />
    </div>
    <div class="mb-3">
        <label>Department</label>
        <select asp-for="DepartmentId" class="form-control">
            <option value="">-- Select Department --</option>
            @foreach (var dept in ViewBag.Departments)
            {
                if (dept.Id == Model.DepartmentId)
                {
                    <option value="@dept.Id" selected>@dept.Name</option>
                }
                else
                {
                    <option value="@dept.Id">@dept.Name</option>
                }
            }
        </select>
    </div>

    <button type="submit" class="btn btn-success">Update</button>
    <a asp-action="getall" class="btn btn-secondary">Cancel</a>
</form> *@



@* lab4
@model lab1mvc.Models.Student
@{
    ViewData["Title"] = "Edit Student";
}

<h2>Edit Student</h2>

<form asp-action="Edit" asp-controller="Student" method="post">
    <input type="hidden" asp-for="SSN" />
    @Html.Partial("_StudentForm", Model)
    <button type="submit" class="btn btn-success">Save Changes</button>
</form>
 *@
